import { ClickedProductCardDirective } from './clicked-product-card.directive';

describe('ClickedProductCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedProductCardDirective();
    expect(directive).toBeTruthy();
  });
});
